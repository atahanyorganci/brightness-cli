[package]
name = "brightness-cli"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4", features = ["derive"] }

[build-dependencies]
bindgen = "0.70"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
